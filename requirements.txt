# Runtime dependencies. Each one is justified in DECISIONS.md — CLAUDE.md 1.1
# forbids adding a dependency without saying why the stdlib was not enough.
apscheduler>=3.10
duckdb>=1.0
exchange_calendars>=4.5
fastapi>=0.111
httpx>=0.27
jinja2>=3.1
pandas>=2.2
pyarrow>=16
pydantic>=2.7
pydantic-settings>=2.2
pywebpush>=2.0
pytz>=2024.1          # duckdb needs it to return TIMESTAMPTZ values to Python
pyyaml>=6.0
tradingview-screener>=2.5
uvicorn[standard]>=0.30
websockets>=12
